import React from "react";

import Nav from "../components/Navigation/Navigation";

export default {
  title: "Navigation Menu",
};

export const Navigation = (selectedKey) => <Nav selectedKey="home"></Nav>;
